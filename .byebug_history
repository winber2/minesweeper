q
@board.grid.each { |el| p el[0].bomb }
@board.grid.each { |el| p el.bomb }
@board.grid.each { |el| p el }
@board.grid.each { |el| puts el if el.bomb }
@board[0]
@board.each { |el| puts el if el.bomb }
board.each { |el| puts el if el.bomb }
self.each { |el| puts el if el.bomb }
self.each do | |el|{ |el| puts el if el.bomb }
self
c
